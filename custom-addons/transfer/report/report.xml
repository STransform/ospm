<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="action_report_transfer_request" model="ir.actions.report">
        <field name="name">Transfer Report</field>
        <field name="model">transfer.request</field>
        <field name="report_type">qweb-pdf</field>
        <field name="report_name">transfer.transfer_request</field> <!-- Must match
        template -->
        <field name="report_file">transfer.transfer_request</field> <!-- Must match
        template -->
        <field name="binding_model_id" ref="model_transfer_request" />
        <field name="binding_type">report</field>
    </record>

</odoo>
