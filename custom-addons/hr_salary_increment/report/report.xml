<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="action_report_hr_salary_increment" model="ir.actions.report">
        <field name="name">Salary Increment Report</field>
        <field name="model">hr.salary.increment.batch</field>
        <field name="report_type">qweb-pdf</field>
        <field name="report_name">hr_salary_increment.report_hr_salary_increment</field> <!-- Must match
        template -->
        <field name="report_file">hr_salary_increment.report_hr_salary_increment</field> <!-- Must match
        template -->
        <field name="binding_model_id" ref="model_hr_salary_increment_batch" />
        <field name="binding_type">report</field>
    </record>

</odoo>